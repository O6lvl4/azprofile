{
  "name": "azprofile",
  "version": "1.0.0",
  "description": "Azure CLI Profile Manager - Easy switching between multiple Azure accounts",
  "bin": {
    "azprofile": "./azprofile"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["azure", "cli", "profile", "account", "switcher"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "zx": "^7.2.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}